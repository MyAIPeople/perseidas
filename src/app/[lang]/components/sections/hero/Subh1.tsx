import { caveat, roboto_mono } from "@/app/[lang]/fonts";
import { cn } from "@/lib/utils";

const Subh1 = () => {
  return <h2 className={cn(caveat.className, "text-yellow-600 text-8xl")}>Experience</h2>;
};

export default Subh1;
